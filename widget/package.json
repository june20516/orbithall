{
  "name": "orbithall-widget",
  "version": "1.0.0",
  "module": "src/main.tsx",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nodemon --watch src --ext ts,tsx,css --exec 'bun build src/main.tsx --outdir ../static --env ORB_PUBLIC_* --format iife --target browser && mv ../static/main.js ../static/embed.js && mv ../static/main.css ../static/embed.css'",
    "build": "NODE_ENV=production bun build src/main.tsx --outdir ../static --env ORB_PUBLIC_* --minify --format iife --target browser && mv ../static/main.js ../static/embed.js && mv ../static/main.css ../static/embed.css",
    "publish": "bun run scripts/publish.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "nodemon": "^3.1.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "preact": "^10.27.2"
  }
}
